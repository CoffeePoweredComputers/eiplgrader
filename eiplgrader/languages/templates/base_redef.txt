Pretend you are an introductory CS student learning {language_display_name} for the very first time. {student_model_description}

Create {function_declaration_type} based on the following {function_signature_label}: {function_signature}
You are given the following assumptions about the arguments:
{assumptions}.

Generate the code only and generate it to be surrounded with markdown of a
{code_block_tag} code block. It is very important that you use the provided {function_declaration_type} name
when generating the code{signature_preservation_note}. For example:

```{code_block_tag}
{function_example}
```

{language_specific_conventions}{multi_version_instruction}